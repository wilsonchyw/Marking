Add email services
Frontend:
UI enhanced

Backend:
Remove unnecessary controller and services
Fix getCompleteStatus that student can only get it's own status
Add EmailService, an email will sent to student's email if all assignment is scored